/* Generated by k2c from runtime/icon_sheet_props.kry. */
#ifndef K_RUNTIME_ICON_SHEET_PROPS_H
#define K_RUNTIME_ICON_SHEET_PROPS_H

#include <stdint.h>
#include <stdbool.h>
#include "kry_bounds.h"

#ifndef KIR_STRING_VALUE_DEFINED
#define KIR_STRING_VALUE_DEFINED
#include <stddef.h>
#include <string.h>
/* Immutable borrowed UTF-8 bytes; length preserves embedded nulls. */
typedef struct String { const char *data; size_t length; } String;
static inline String StringView(const char *data, size_t length) {
    String value = {data, length};
    return value;
}
static inline bool StringEqual(String a, String b) {
    return a.length == b.length && (a.length == 0 || memcmp(a.data, b.data, a.length) == 0);
}
#endif

typedef enum IconSheet {
    ICON_SHEET_CORE = 0,
    ICON_SHEET_PFP = 1,
    ICON_SHEET_PLATFORMS = 2,
    ICON_SHEET_PAYMENTS = 3,
    ICON_SHEET_LANGUAGE = 4,
    ICON_SHEET_TILES = 5,
    ICON_SHEET_LOGOS = 6,
    ICON_SHEET_COUNT = 7,
} IconSheet;

#endif /* K_RUNTIME_ICON_SHEET_PROPS_H */
