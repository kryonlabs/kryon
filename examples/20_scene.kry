#module "example_scene"
#import "kryon.h"
#import "kryon_example_font.h"
#style <material> as material

app "Kryon Scene Tree" {
    size 640 420
    fps 60
    font examples
    scene main
}

sprite_id :: int #global #export

scene main {
    camera := NodeCreate(scene, 0, NodeKindCamera2D, "camera")
    NodeSetProps(scene, camera, Camera2DPropsAlloc(1.0, 1))

    sprite_id = NodeCreate(scene, 0, NodeKindSprite2D, "player")
    NodeSetPosition(scene, sprite_id, 320.0, 210.0)
    NodeSetProps(scene, sprite_id, Sprite2DPropsAlloc("tiles/tile.png", 96.0, 96.0))
}
