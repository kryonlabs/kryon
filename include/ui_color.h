#ifndef UI_COLOR_H
#define UI_COLOR_H

#include "kryon.h"

// Lighten a color by adding to each RGB componentColor LightenUIColor(Color c, int amount);

// Darken a color by subtracting from each RGB componentColor DarkenUIColor(Color c, int amount);

#endif // UI_COLOR_H