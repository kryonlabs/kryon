/* Generated by k2c from runtime/popup_props.kry. */
#ifndef K_RUNTIME_POPUP_PROPS_H
#define K_RUNTIME_POPUP_PROPS_H

#include <stdint.h>
#include <stdbool.h>

#ifndef KIR_STRING_VALUE_DEFINED
#define KIR_STRING_VALUE_DEFINED
#include <stddef.h>
#include <string.h>
/* Immutable borrowed UTF-8 bytes; length preserves embedded nulls. */
typedef struct String { const char *data; size_t length; } String;
static inline String StringView(const char *data, size_t length) {
    String value = {data, length};
    return value;
}
static inline bool StringEqual(String a, String b) {
    return a.length == b.length && (a.length == 0 || memcmp(a.data, b.data, a.length) == 0);
}
#endif
#include "kryon_compat.generated.h"

typedef enum PopupFlags {
    PopupFlagsNone = 0,
    PopupTooltip = 1,
    PopupModal = 2,
    PopupContext = 4,
} PopupFlags;

typedef struct PopupProps {
    Rectangle bounds;
    int32_t id;
    int32_t class_name;
    bool* open;
    bool disabled;
    Rectangle trigger;
    PopupFlags flags;
} PopupProps;

#endif /* K_RUNTIME_POPUP_PROPS_H */
