/* Generated by k2c from runtime/slider_props.kry. */
#ifndef K_RUNTIME_SLIDER_PROPS_H
#define K_RUNTIME_SLIDER_PROPS_H

#include <stdint.h>
#include <stdbool.h>
#include "kry_bounds.h"

#ifndef KIR_STRING_VALUE_DEFINED
#define KIR_STRING_VALUE_DEFINED
#include <stddef.h>
#include <string.h>
/* Immutable borrowed UTF-8 bytes; length preserves embedded nulls. */
typedef struct String { const char *data; size_t length; } String;
static inline String StringView(const char *data, size_t length) {
    String value = {data, length};
    return value;
}
static inline bool StringEqual(String a, String b) {
    return a.length == b.length && (a.length == 0 || memcmp(a.data, b.data, a.length) == 0);
}
#endif
#include "ui_input_props.generated.h"

typedef struct SliderProps {
    Rectangle bounds;
    int32_t id;
    const char* label;
    NumericValueKind kind;
    float* float_values;
    int32_t* int_values;
    int32_t value_count;
    float* float_value;
    double min;
    double max;
    const char* format;
    bool disabled;
    bool vertical;
    bool angle;
    int32_t class_name;
    double value_scale;
    const char* value_format;
    bool step_buttons;
    bool show_limits;
    float* measured_height;
} SliderProps;

#endif /* K_RUNTIME_SLIDER_PROPS_H */
