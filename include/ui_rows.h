#ifndef KRYON_ROWS_H
#define KRYON_ROWS_H

/*
 * Public row/form composites were removed from the widget surface. Compose
 * forms from canonical .kry layout plus Text, TextField, Checkbox, Spinbox,
 * and Button instead. Native row helpers remain internal host support only.
 */

#endif
