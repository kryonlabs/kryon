#module "example_images"
#import "kryon.h"
#style <material> as material
#import "kryon_example_font.h"

app "Kryon Images" {
    size 640 420
    fps 60
    font examples
}

App :: () #ui {
    Screen root: {
        AppBackground()
        Text((TextProps){.bounds={Scale(32), Scale(30), 0, 0}, .text="Images are image nodes", .wrap=TextWrapNone})
        Stack image_card: {
            bounds = {Scale(32), Scale(82), Scale(260), Scale(210)}
            key = Key("image-card")
            Box((Rectangle){Scale(32), Scale(82), Scale(260), Scale(210)},
                       GetThemeSurface(), GetThemeButton())
            Image((ImageProps){.asset_path = "tiles/tile.png",
                     .bounds = {Scale(52), Scale(104), Scale(96), Scale(96)},
                     .source = {0, 0, 0, 0}, .origin = {0, 0}, .rotation = 0.0f,
                     .tint = WHITE, .fit = ImageFitContain})
            Image((ImageProps){.asset_path = "pfp/lotus.png",
                     .bounds = {Scale(168), Scale(104), Scale(96), Scale(96)},
                     .source = {0, 0, 0, 0}, .origin = {0, 0}, .rotation = 0.0f,
                     .tint = WHITE, .fit = ImageFitContain})
            Text((TextProps){.bounds={Scale(52), Scale(226), 0, 0}, .text="Images keep their node bounds for editing", .color=GetThemeIcon(), .wrap=TextWrapNone})
        }
    }
}
