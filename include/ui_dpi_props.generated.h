/* Generated by k2c from runtime/dpi_props.kry. */
#ifndef K_RUNTIME_DPI_PROPS_H
#define K_RUNTIME_DPI_PROPS_H

#include <stdint.h>
#include <stdbool.h>
#include "kry_bounds.h"

#ifndef KIR_STRING_VALUE_DEFINED
#define KIR_STRING_VALUE_DEFINED
#include <stddef.h>
#include <string.h>
/* Immutable borrowed UTF-8 bytes; length preserves embedded nulls. */
typedef struct String { const char *data; size_t length; } String;
static inline String StringView(const char *data, size_t length) {
    String value = {data, length};
    return value;
}
static inline bool StringEqual(String a, String b) {
    return a.length == b.length && (a.length == 0 || memcmp(a.data, b.data, a.length) == 0);
}
#endif

typedef struct DPIState {
    int physical_width;
    int physical_height;
    int view_width;
    int view_height;
    int layout_width;
    int layout_height;
    float ui_scale;
    float ui_scale_clamped;
    float render_scale;
    float camera_zoom;
    int base_width;
    int base_height;
    int needs_update;
} DPIState;

#endif /* K_RUNTIME_DPI_PROPS_H */
