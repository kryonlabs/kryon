#module "accelerator"

Accelerator :: struct {
    key: i32
    ctrl: i32
    shift: i32
    alt: i32
    id: i32
}

AcceleratorPressed :: (accelerator: Accelerator) -> i32 #extern #export
DispatchAccelerators :: (accelerators: const Accelerator*, count: i32) -> i32 #extern #export
