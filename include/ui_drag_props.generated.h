/* Generated by k2c from runtime/drag_props.kry. */
#ifndef K_RUNTIME_DRAG_PROPS_H
#define K_RUNTIME_DRAG_PROPS_H

#include <stdint.h>
#include <stdbool.h>

#ifndef KIR_STRING_VALUE_DEFINED
#define KIR_STRING_VALUE_DEFINED
#include <stddef.h>
#include <string.h>
/* Immutable borrowed UTF-8 bytes; length preserves embedded nulls. */
typedef struct String { const char *data; size_t length; } String;
static inline String StringView(const char *data, size_t length) {
    String value = {data, length};
    return value;
}
static inline bool StringEqual(String a, String b) {
    return a.length == b.length && (a.length == 0 || memcmp(a.data, b.data, a.length) == 0);
}
#endif
#include "ui_input_props.generated.h"

typedef enum DragMode {
    DragValue = 0,
    DragRange = 1,
} DragMode;

typedef struct DragProps {
    Rectangle bounds;
    int32_t id;
    const char* label;
    NumericValueKind kind;
    DragMode mode;
    float* float_values;
    int32_t* int_values;
    int32_t value_count;
    float* float_min;
    float* float_max;
    int32_t* int_min;
    int32_t* int_max;
    float speed;
    double min;
    double max;
    const char* format;
    const char* format_max;
    bool disabled;
} DragProps;

#endif /* K_RUNTIME_DRAG_PROPS_H */
