/* Generated by k2c from runtime/scene_tree_props.kry. */
#ifndef K_RUNTIME_SCENE_TREE_PROPS_H
#define K_RUNTIME_SCENE_TREE_PROPS_H

#include <stdint.h>
#include <stdbool.h>
#include "kry_bounds.h"

#ifndef KIR_STRING_VALUE_DEFINED
#define KIR_STRING_VALUE_DEFINED
#include <stddef.h>
#include <string.h>
/* Immutable borrowed UTF-8 bytes; length preserves embedded nulls. */
typedef struct String { const char *data; size_t length; } String;
static inline String StringView(const char *data, size_t length) {
    String value = {data, length};
    return value;
}
static inline bool StringEqual(String a, String b) {
    return a.length == b.length && (a.length == 0 || memcmp(a.data, b.data, a.length) == 0);
}
#endif

typedef enum NodeKind {
    NodeKindRoot = 0,
    NodeKindNode2D = 1,
    NodeKindCamera2D = 2,
    NodeKindSprite2D = 3,
    NodeKindAnimatedSprite2D = 4,
    NodeKindTileMap = 5,
    NodeKindCollisionShape2D = 6,
    NodeKindArea2D = 7,
    NodeKindBody2D = 8,
    NodeKindAnimationPlayer = 9,
    NodeKindAudioSource = 10,
    NodeKindLight2D = 11,
    NodeKindCustom = 12,
} NodeKind;

typedef enum NodeFlag {
    NodeFlagAlive = 1,
    NodeFlagReady = 2,
    NodeFlagDirty = 4,
} NodeFlag;

#endif /* K_RUNTIME_SCENE_TREE_PROPS_H */
