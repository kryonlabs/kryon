/* Generated by k2c from runtime/canvas_props.kry. */
#ifndef K_RUNTIME_CANVAS_PROPS_H
#define K_RUNTIME_CANVAS_PROPS_H

#include <stdint.h>
#include <stdbool.h>
#include "kry_bounds.h"

#ifndef KIR_STRING_VALUE_DEFINED
#define KIR_STRING_VALUE_DEFINED
#include <stddef.h>
#include <string.h>
/* Immutable borrowed UTF-8 bytes; length preserves embedded nulls. */
typedef struct String { const char *data; size_t length; } String;
static inline String StringView(const char *data, size_t length) {
    String value = {data, length};
    return value;
}
static inline bool StringEqual(String a, String b) {
    return a.length == b.length && (a.length == 0 || memcmp(a.data, b.data, a.length) == 0);
}
#endif
#include "kryon_compat.generated.h"

typedef struct Canvas {
    Rectangle bounds;
    int32_t class_name;
    int32_t* scroll_x;
    int32_t* scroll_y;
    float* zoom;
} Canvas;

typedef struct CanvasResult {
    bool active;
    bool dragging;
    int32_t selected_index;
    Vector2 world;
} CanvasResult;
int32_t CanvasHitTest(Vector2 point, Rectangle*  items, int32_t  item_count);
Vector2 CanvasToScreen(Canvas canvas, Vector2  point);
Rectangle CanvasRectToScreen(Canvas canvas, Rectangle  rect);

#endif /* K_RUNTIME_CANVAS_PROPS_H */
