/* Generated by k2c from runtime/swipe_props.kry. */
#ifndef K_RUNTIME_SWIPE_PROPS_H
#define K_RUNTIME_SWIPE_PROPS_H

#include <stdint.h>
#include <stdbool.h>
#include "kry_bounds.h"

#ifndef KIR_STRING_VALUE_DEFINED
#define KIR_STRING_VALUE_DEFINED
#include <stddef.h>
#include <string.h>
/* Immutable borrowed UTF-8 bytes; length preserves embedded nulls. */
typedef struct String { const char *data; size_t length; } String;
static inline String StringView(const char *data, size_t length) {
    String value = {data, length};
    return value;
}
static inline bool StringEqual(String a, String b) {
    return a.length == b.length && (a.length == 0 || memcmp(a.data, b.data, a.length) == 0);
}
#endif
#include "ui_drawing_props.generated.h"

typedef enum SwipeDirection {
    SwipeNone = 0,
    SwipeLeft = 1,
    SwipeRight = 2,
    SwipeUp = 4,
    SwipeDown = 8,
    SwipeHorizontal = 3,
    SwipeVertical = 12,
    SwipeAll = 15,
} SwipeDirection;

typedef struct SwipeSpec {
    Rectangle bounds;
    uint32_t directions;
    float min_distance;
    float axis_bias;
    float max_duration;
} SwipeSpec;

typedef struct SwipeGesture {
    int32_t active;
    int32_t dragging;
    int32_t cancelled;
    Vector2 start;
    double started_at;
} SwipeGesture;

typedef struct SwipeResult {
    SwipeDirection direction;
    Vector2 delta;
    float progress;
    int32_t active;
    int32_t dragging;
    int32_t cancelled;
} SwipeResult;

#endif /* K_RUNTIME_SWIPE_PROPS_H */
