/* Generated by k2c from runtime/app_host_props.kry. */
#ifndef K_RUNTIME_APP_HOST_PROPS_H
#define K_RUNTIME_APP_HOST_PROPS_H

#include <stdint.h>
#include <stdbool.h>
#include "kry_bounds.h"

#ifndef KIR_STRING_VALUE_DEFINED
#define KIR_STRING_VALUE_DEFINED
#include <stddef.h>
#include <string.h>
/* Immutable borrowed UTF-8 bytes; length preserves embedded nulls. */
typedef struct String { const char *data; size_t length; } String;
static inline String StringView(const char *data, size_t length) {
    String value = {data, length};
    return value;
}
static inline bool StringEqual(String a, String b) {
    return a.length == b.length && (a.length == 0 || memcmp(a.data, b.data, a.length) == 0);
}
#endif
#include "kryon_compat.generated.h"
#define APP_HOST_ABI_VERSION 6

typedef void (*AppRouteEnterFn)(void *app, int route_index);

typedef void (*AppRouteDrawFn)(void *app, Rectangle viewport);

typedef struct { const char *id; const char *group; const char *title; const char *source_path; AppRouteEnterFn enter; AppRouteDrawFn draw; } AppRouteInfo;

typedef AppRouteInfo AppScreenInfo;

typedef int (*HostScreenCountFn)(void *userdata);

typedef AppScreenInfo (*HostScreenFn)(void *userdata, int index);

typedef void (*HostSelectScreenFn)(void *userdata, int index);

typedef int (*HostSelectSourcePathFn)(void *userdata, const char *source_path);

typedef void (*HostDrawFn)(void *userdata, Rectangle viewport);

typedef void (*HostResizeFn)(void *userdata, int width, int height);

typedef void (*HostSetFocusedFn)(void *userdata, int focused);

typedef struct { void *userdata; HostScreenCountFn screen_count; HostScreenFn screen; HostSelectScreenFn select_screen; HostSelectSourcePathFn select_source_path; HostDrawFn draw; HostResizeFn resize; HostSetFocusedFn set_focused; } AppHost;

typedef AppHost *(*CreateAppHostCallback)(int abi_version, const char *project_path);

typedef void (*DestroyAppHostCallback)(AppHost *host);

typedef struct { void *app; const AppRouteInfo *routes; int route_count; int selected_route; } App;

typedef AppRouteInfo AppScreen;

typedef struct KryonInputOverride {
    int32_t enabled;
    int32_t mouse_inside;
    int32_t pass_buttons;
    int32_t pass_keyboard;
    Vector2 mouse_position;
    Vector2 mouse_delta;
} KryonInputOverride;
void BeginKryonInputOverride(KryonInputOverride input);
void EndKryonInputOverride(void);
int32_t IsInspectActive(void);
void ResizeAppHost(AppHost* host, int32_t  width, int32_t  height);
void SetAppHostFocused(AppHost* host, int32_t  focused);
void BindAppHost(App* app, AppHost*  host);
int32_t GetAppScreenCount(AppHost* host);
AppScreenInfo GetAppScreen(AppHost* host, int32_t  index);
void SetAppScreen(AppHost* host, int32_t  index);
int32_t SetAppScreenById(AppHost* host, const char*  id);
int32_t SetAppScreenBySourcePath(AppHost* host, const char*  source_path);
int32_t SetAppScreenFromRoute(AppHost* host);
void PushAppScreenRoute(AppHost* host, int32_t  index);
void ReplaceAppScreenRoute(AppHost* host, int32_t  index);
void DrawAppScreen(AppHost* host, Rectangle  viewport);

#endif /* K_RUNTIME_APP_HOST_PROPS_H */
