# Simple Counter Test
# Tests the complete KRY → KIR → DIS pipeline

component Counter {
    state count: number = 0

    view Box {
        Text("Count: {count}")
        Button("Increment") {
            onClick: count = count + 1
        }
    }
}
