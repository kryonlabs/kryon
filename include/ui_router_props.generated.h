/* Generated by k2c from runtime/router_props.kry. */
#ifndef K_RUNTIME_ROUTER_PROPS_H
#define K_RUNTIME_ROUTER_PROPS_H

#include <stdint.h>
#include <stdbool.h>
#include "kry_bounds.h"

#ifndef KIR_STRING_VALUE_DEFINED
#define KIR_STRING_VALUE_DEFINED
#include <stddef.h>
#include <string.h>
/* Immutable borrowed UTF-8 bytes; length preserves embedded nulls. */
typedef struct String { const char *data; size_t length; } String;
static inline String StringView(const char *data, size_t length) {
    String value = {data, length};
    return value;
}
static inline bool StringEqual(String a, String b) {
    return a.length == b.length && (a.length == 0 || memcmp(a.data, b.data, a.length) == 0);
}
#endif
#include "kryon_compat.generated.h"
#include "kryon_key.h"

typedef struct RouterRoute {
    int32_t id;
    int32_t parent;
    const char* path;
    const char* title;
    const char* group;
} RouterRoute;

typedef struct RouterState {
    int32_t initialized;
    int32_t current_route;
    int32_t previous_route;
    int32_t requested_route;
    int32_t changed;
    int32_t route_version;
    uint32_t generation;
} RouterState;

typedef struct RouterProps {
    Rectangle bounds;
    KeyID key;
    RouterState* state;
    const RouterRoute* routes;
    int32_t route_count;
    int32_t initial_route;
    bool sync_url;
    bool replace_on_init;
} RouterProps;

typedef struct RouterResult {
    int32_t route;
    int32_t previous_route;
    int32_t requested_route;
    bool changed;
    const RouterRoute* route_info;
} RouterResult;

#endif /* K_RUNTIME_ROUTER_PROPS_H */
